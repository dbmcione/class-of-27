/**
 * The game's name, in one place: the landing page, the browser tab, the
 * scorecard and the share sheet all read it from here, so a second copy
 * cannot drift from the first.
 *
 * The Class of '27 wording survives only in the address, which is not copy
 * and cannot be changed without breaking every link already shared.
 */
export const CHALLENGE_NAME = 'Spot the Diagnosis';

export const CHALLENGE_DESCRIPTION =
  'Can you spot the diagnosis? Solve the puzzle, see how NEET PG tests the ' +
  'topic and find out where you stand against your college.';
